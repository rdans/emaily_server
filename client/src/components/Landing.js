import React from 'react';

//another curly bracket inside curly bracket attribute's value mean passing javascript code
const Landing = () => {
  return(
      <div style={{ textAlign: 'center' }}>
        <h1>
          Emaily!
        </h1>
        Collect feedback from your users
      </div>

  );
};

export default Landing;
